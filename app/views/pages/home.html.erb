<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
  <!-- Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      padding-top: 60px; /* Adjust body padding to accommodate fixed navbar */
      background-image: url("https://images.unsplash.com/photo-1509077613385-f89402467146?q=80&w=1527&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D");
      background-size: cover;
      background-position: center;
      margin: 0;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .navbar {
      background-color: rgba(0,0,0,0.7); /* Add transparency to navbar */
    }
    .hello{
      margin-left:15rem;
    }
    .container {
      border-radius: 10px;
      padding: 20px;
      text-align: center;
      margin-top:10rem;
    }
    .container h1,
    .container p {
      color: white;
      font-size: 24px;
      font-weight: bold;
    }
    .nav item{
      font-weight: bold;
    }

  </style>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
    <a class="navbar-brand" href="#">Shantinagar Futsal</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse"
      aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="hello navbar-nav mr-auto">
        <% if current_user %>
          <li class="nav-item">
            <a class="nav-link" href="#">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/bookings/1">Bookings</a>
          </li>
           <li class="nav-item">
            <a class="nav-link" href="#">Registration</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Shopping</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">About Us</a>
          </li>
        <% end %>
      </ul>
      <ul class="navbar-nav ml-auto">
        <% if current_user %>
          <li class="nav-item">
            <a class="nav-link" href="#">Hello <%= current_user.name %>!</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">You are <%= current_user.role %></a>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Out", destroy_user_session_path, 'data-turbo-method': :delete, class: "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Sign Up", new_user_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </nav>

  <!-- Page Content -->
  <div class="container">
    
    <% if current_user %>
   
 <a class="nav-link" href="/bookings/1">Bookings</a>
    <% else %>
    <h1 class="mt-5">WELCOME TO OUR WEBSITE</h1>
    <p class="lead">LETS PLAY FUTSAL</p>
      <p>GET STARTED BY SIGNING UP AND LOGGING IN </p>
    <% end %>
  </div>

  <!-- Bootstrap JS (optional, if you need dropdowns or other components) -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body> 
</html>

